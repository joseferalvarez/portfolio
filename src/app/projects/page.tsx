'use client'

export default function page({ params }: { params: any }) {
    return (
        <>
            <div>projects</div>
            <button onClick={() => { console.log('hola') }}></button>
        </>
    )
}
